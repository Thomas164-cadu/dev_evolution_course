/**
 * No package.json o funcionamento das versões dos pacotes
 

Patch releases : ~1.0.4
Minor releases : ^1.0.4
Major releases : *1.0.4

*/

/**
 * O package-lock.json auxilia o package.json por conter a árvore de dependências dos pacotes, e bloqueia as atualizações de versões para a aplicação 
 */

/**
 * npm update = atualiza pacotes
 * npm audit = verifica bugs de segurança, o npm audit fix resolve esses problemas
 */

/**
 * O HTTP o node já entrega para o desenvolvedor
 */

const http = require('http'); //exemplo no app da pasta node

/**
 * Nos métodos de resposta do node, a rota res.download permite fazer o download de um arquivo de sua aplicação
 */